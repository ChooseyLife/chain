{
  "name": "hardhat-project",
  "scripts": {
    "local": "npx hardhat node",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat clean && npx hardhat compile && npx hardhat run scripts/deploy.ts --network mumbai"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.13.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.2",
    "dotenv": "^16.0.3"
  }
}
